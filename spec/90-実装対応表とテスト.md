# 実装対応表とテスト

## 1. 調査対象

| 対象 | 規模／役割 |
| --- | --- |
| `app.js` | 15,977行。状態、UI、作図、Block、Presentation、保存、テストフックを含む |
| `geometry_kernel.js` | 230行。UIとSolverで共有するpureな角度、Arc端点・sweep・parameter、Line方向・投影・交点・反射・距離計算 |
| `geometry_ref.js` | 通常GeometryとBlock Projectionの不変な参照値、ID／key codec、lookup非依存resolver |
| `constraint_solver.js` | 1,564行。Geometry 型、Constraint 型、線形代数、solve、自由度解析 |
| `index.html` | ツール、Canvas、Sketch、Block、サイドバー |
| `style.css` | モード・Editor・レスポンシブレイアウト |
| `tests/e2e/*.spec.js` | 7ファイル、Playwright 108 tests。データ駆動テストとPhase 0特性テストを含む |
| `tests/unit/*.test.js` | Node標準`node:test`によるGeometry ref／kernel／Solver unit test 22件 |
| `test-data/*.json` | 複雑・完全拘束 fixture |

基準 commit は `5beaefc` である。

Phase 0追加前の検証結果:

- `npm run check`: 成功
- `npm run test:e2e`: 86 tests passed（1.8分）

Phase 0では、製品コードを変更せず次の安全網を追加した。

- 全永続Constraint型、Sketch階層、入れ子Block、複数Presentation Sheetを含むv8 fixtureの意味的round-trip
- 代表v1 fixtureのv8正規化、再round-trip、ID採番予約
- Geometry／Block／Definition／内部Sketch無効化の参照ライフサイクル
- 通常履歴、Block Editorローカル履歴、キャンセル、Undo/Redo分岐
- draw、hit test、hover、viewport、sidebar更新のモデル非破壊性
- Presentation操作がGeometry、Constraint、solveへ干渉しないこと
- 全Playwright pageの未処理例外と`console.error`監視
- Geometry、入れ子Block、Presentation、Block EditorのCanvas visual baseline 4件
- Geometry primitive、代表Constraint残差、Solver自由度のunit test 3件

## 2. 機能と実装位置

| 領域 | 主な実装 | テスト | 状態 |
| --- | --- | --- | --- |
| Document 初期状態 | [`app.js`](../app.js#L62) | 各E2E初期化 | 実装済み |
| Sketch 正規化・階層 | [`app.js`](../app.js#L286), [`app.js`](../app.js#L1268) | `presentation-annotations.spec.js` 777〜947付近 | 実装済み |
| 参照拘束と依存 solve | [`app.js`](../app.js#L1346), [`app.js`](../app.js#L6594) | 同 868〜947付近 | 実装済み |
| Geometry 型 | [`constraint_solver.js`](../constraint_solver.js#L59) | 全 Geometry E2E | 実装済み |
| Geometry math kernel | [`geometry_kernel.js`](../geometry_kernel.js) | `geometry-kernel.test.js`、角度／距離／投影／交点／反射／Arc sweep・parameter関連E2E | R-012完了 |
| Geometry reference codec | [`geometry_ref.js`](../geometry_ref.js) | `geometry-ref.test.js`、round-trip、Presentation、Constraint、入れ子Block E2E | R-006完了 |
| Constraint 型 | [`constraint_solver.js`](../constraint_solver.js#L133) | authoring、drag、presentation-annotations | 実装済み |
| Solver | [`constraint_solver.js`](../constraint_solver.js#L970) | adversarial fixture / performance / drag | 実装済み |
| 作図・編集 | [`app.js`](../app.js#L11743), [`app.js`](../app.js#L11840) | authoring performance、presentation-annotations | 実装済み |
| local/guided drag | [`app.js`](../app.js#L11124) | drag-smoothness、geometry-drag-smoothness | 実装済み |
| 保存・読込 v8 | [`app.js`](../app.js#L3590), [`app.js`](../app.js#L3986) | blocks、authoring、test hook経由 | 実装済み |
| Undo/Redo | [`app.js`](../app.js#L3676) | presentation-annotations、blocks | 実装済み |
| Block Projection | [`app.js`](../app.js#L709), [`app.js`](../app.js#L741) | `blocks.spec.js` | 実装済み |
| 入れ子 Block | [`app.js`](../app.js#L2444), [`app.js`](../app.js#L2535) | `blocks.spec.js` 855〜1450付近 | 実装済み |
| Presentation Sheet | [`app.js`](../app.js#L313), [`app.js`](../app.js#L9631) | レイアウトの一部 | 部分的 |
| Geometry style | [`app.js`](../app.js#L874), [`app.js`](../app.js#L7517) | 直接的なUI回帰が不足 | 部分的 |
| Annotation Dimension | [`app.js`](../app.js#L953), [`app.js`](../app.js#L7979) | drag 1件、寸法共通描画テスト | 部分的 |
| Leader | [`app.js`](../app.js#L1111), [`app.js`](../app.js#L8018) | drag 1件 | 部分的 |
| Presentation 可視 Sketch | [`app.js`](../app.js#L327) | なし | データのみ、未接続 |
| Presentation Element 選択・削除 | [`app.js`](../app.js#L8071), [`app.js`](../app.js#L12626) | なし | ドラッグのみ |

## 3. 自動テストの守備範囲

### 強い領域

- 複雑な完全拘束 fixture と自由度解析
- 拘束付き Point、Line、Circle、Arc、Arc 端点のドラッグ
- 作図・拘束追加の性能上限
- 対称、接線、角度、寸法表示、トリム、オフセット
- Sketch 表示、削除、先祖参照、循環・範囲外参照
- Block 作成、配置、回転、構成、保存、入れ子、Editor 履歴、ロールバック
- コピー、切り取り、貼り付け、Undo/Redo
- v8全永続Constraint型の意味的round-tripと代表v1互換
- Block Editorキャンセル／完了と通常履歴の意味的状態一致
- 描画・hit test・viewport操作のモデル非破壊性
- Presentation操作からGeometry／solveへの非干渉
- 4場面のCanvas visual regression

### 弱い領域

- Presentation Sheet の追加、名前変更、切替、保存を一連のUI操作で確認するテスト
- Geometry style の全プロパティ、複数選択、Sheet 間分離、Block Projection への適用
- 注記寸法の全対象組み合わせを UI から作成・保存・再読込するテスト
- 水平／垂直 Point 寸法の Presentation 保存
- Leader の作成、キャンセル、Geometry 追従、再読込
- Presentation Element の削除・style 編集。機能自体が未実装
- Geometry 削除時の参照切れ Presentation UX
- `visibleGeometrySketchIds`。機能自体が描画へ未接続
- JSON schema 境界値、ID 重複、未知 version の明示的テスト

### Phase 0で検出した既知ギャップ

次は製品挙動を変更しないPhase 0の範囲外とし、テストではexpected-failureまたは厳密なallowlistとして可視化している。

1. 直接Geometryを削除するとConstraintは除去されるが、Presentation `elementStyles` と注記Elementが残る。
2. Line作図の開始点をEscで取り消すとモデルは戻るが、履歴エントリが1件残る。
3. 角度寸法の未使用`offsetU`／`offsetN`は保存時`null`、再読込後`0`となり、意味は同じでもJSON形状が1回変化する。
4. 拘束対象の段階選択中、未完成operandから一時Constraintを組み立てて`TypeError`を発生させる経路がある。runtime監視は、同一のメッセージと`constraintFromSelection`経路が一致する場合だけ既知事象として許可する。

### Phase 0テスト対応表

| 契約 | Unit | State/Contract | 実UI E2E | Visual |
| --- | --- | --- | --- | --- |
| Geometry／Solver | 代表形状・拘束・自由度 | fixture解析 | 既存authoring／drag | Geometry／拘束状態 |
| 保存・読込 | — | v8意味的round-trip、v1互換、ID予約 | 実File読込経路 | — |
| 参照ライフサイクル | — | Geometry、Instance、Definition、内部Sketch | 削除・無効化・Undo | 入れ子Block |
| 履歴／EditingScope | — | transaction、分岐、cancel、commit | Undo/RedoボタンとBlock Editor | Block Editor |
| Presentation境界 | — | Geometry／solve非干渉 | Sheet、style、注記drag | Presentation注記 |
| Adapter非破壊性 | — | draw、hit、hover、viewport、sidebar | Pointer／resize／wheel | 4 baseline |
| Runtime error | — | — | 全Playwright pageで監視 | — |

2026-08-12の同一Windows環境でのPhase 0実測結果（成功した最終検証2回で観測した最大値）:

- `npm run test:all`: 成功（構文確認、unit 3件、Playwright 103件。E2Eは1.5〜1.7分）
- authoring command最大153ms（上限250ms）、Geometry作成最大100ms（上限350ms）
- Constraint作成最大125ms、Dimension作成最大117ms、接続Constraint作成最大105ms（各上限350ms）
- 編集操作最大117ms（上限350ms）、選択／履歴／表示操作最大216ms（操作別上限150〜500ms）、Geometry選択最大49ms（上限150ms）
- drag系の全データ駆動caseは既存の反復回数、経過時間、jump ratio上限を通過した

将来の性能変更では、同じ実行環境・worker数で再計測し、既存の絶対上限に加えて基準中央値から15%以上悪化した項目を調査対象とする。

R-012第一境界の検証結果（2026-08-12）:

- `npm run test:all`: 成功（unit 6件、Playwright 103件、E2E 1.7分）
- 全性能テストが既存の絶対上限を通過した
- 並列全件実行でGeometry選択の単発最大値が57msとなったため直列3回を再計測し、48ms、49ms、49ms、中央値49msでPhase 0基準と一致することを確認した

R-012第二境界の検証結果（2026-08-15）:

- `npm run test:all`: 成功（unit 9件、Playwright 106件、E2E 1.9分）
- 全性能テストが既存の絶対上限を通過した
- 並列全件実行でGeometry作成の単発最大値が116msとなったため直列3回を再計測し、115ms、100ms、100ms、中央値100msでPhase 0基準と一致することを確認した

R-012第三境界の検証結果（2026-08-15）:

- `npm run test:all`: 成功（unit 11件、Playwright 106件、E2E 1.9〜2.0分。最終実装で再実行済み）
- 全性能テストが既存の絶対上限を通過した
- 並列全件実行2回でGeometry作成が最大164ms、Geometry選択が最大59msとなったため各項目を直列3回で再計測した。Geometry作成は100ms、99ms、100ms、中央値100ms、Geometry選択は48ms、61ms、49ms、中央値49msとなり、いずれもPhase 0基準と一致することを確認した
- 最終並列実行でauthoring commandが198ms、Constraint作成が150ms、接続Constraint作成が132msとなったため、それぞれ直列3回で再計測した。中央値は133ms、115ms、109msとなり、Phase 0基準から15%以上の悪化がないことを確認した

R-012第四境界の検証結果（2026-08-15）:

- `npm run test:all`: 成功（unit 12件、Playwright 108件、E2E 2.2分）
- 全性能テストが既存の絶対上限を通過した
- 並列全件実行で基準比15%以上となった8群を直列3回で再計測した。authoring command、Constraint作成、Dimension作成、接続Constraint作成、編集、選択／履歴／表示、Geometry選択の中央値はそれぞれ133ms、120ms、133ms、116ms、133ms、215ms、52msで、継続的な15%以上の悪化がないことを確認した
- Geometry作成は最初の直列計測で中央値118msとなったため、旧ローカル交点経路と最終共通経路を同じ条件で追加比較した。旧経路116ms、共通経路117msで差は1%未満となり、共通化による悪化ではないことを確認した

R-012第五境界の検証結果（2026-08-15）:

- `npm run test:all`: 成功（unit 13件、Playwright 108件、E2E 2.2分）
- 全性能テストが既存の絶対上限を通過した
- 並列全件実行で基準比15%以上となった8群を直列3回で再計測した。authoring command、Geometry作成、Constraint作成、Dimension作成、接続Constraint作成、編集、選択／履歴／表示、Geometry選択の中央値はそれぞれ118ms、116ms、117ms、135ms、116ms、134ms、216ms、53msだった
- Phase 0比15%以上となるGeometry作成とDimension作成は、直前の第四境界で確認した現行中央値117ms、133msとの差がそれぞれ1ms、2msであり、第五境界による悪化ではないことを確認した

R-012第六境界（完了）の検証結果（2026-08-15）:

- `npm run test:all`: 成功（unit 16件、Playwright 108件、E2E 2.1分）
- 全性能テストが既存の絶対上限を通過した
- 並列全件実行で基準比15%以上となった8群を直列3回で再計測した。authoring command、Geometry作成、Constraint作成、Dimension作成、接続Constraint作成、編集、選択／履歴／表示、Geometry選択の中央値はそれぞれ134ms、116ms、132ms、132ms、118ms、133ms、198ms、58msだった
- Phase 0比15%以上となるGeometry作成とGeometry選択は、直前の第五境界の中央値116ms、53msに対してそれぞれ同値、約9%増であり、今回のArc共通化による15%以上の悪化ではないことを確認した

R-006第一境界の検証結果（2026-08-15）:

- `npm run test:all`: 成功（unit 21件、Playwright 108件、E2E 1.8分）
- 全性能テストが既存の絶対上限を通過した
- Projection生成への値オブジェクト導入を考慮し、性能8群を直列3回で再計測した。authoring command、Geometry作成、Constraint作成、Dimension作成、接続Constraint作成、編集、選択／履歴／表示、Geometry選択の中央値はそれぞれ101ms、101ms、99ms、103ms、100ms、120ms、182ms、49msだった
- すべてR-012完了時の中央値以下であり、GeometryRef共通化による15%以上の悪化がないことを確認した

R-006第二境界（完了）の検証結果（2026-08-15）:

- `npm run test:all`: 成功（unit 22件、Playwright 108件、E2E 1.9分）
- 全性能テストが既存の絶対上限を通過した
- 並列全件実行の性能8群はauthoring command 165ms、Geometry作成150ms、Constraint作成150ms、Dimension作成124ms、接続Constraint作成125ms、編集134ms、選択／履歴／表示267ms、Geometry選択56msだった
- 第一境界基準から15%以上となった6群を直列3回で再計測した。authoring command、Geometry作成、Constraint作成、Dimension作成、接続Constraint作成、選択／履歴／表示の中央値はそれぞれ118ms、99ms、100ms、104ms、107ms、182msだった
- authoring commandだけは第一境界の101msを15%以上上回ったため、変更前commitを同じ環境で直列3回計測した。変更前117ms、変更後118msで差は1ms未満となり、Constraint参照共通化による悪化ではないことを確認した

`presentation-annotations.spec.js` という名前のファイルは Geometry・Sketch・UI の回帰テストも多数含み、Presentation 専用テスト群にはなっていない。

## 4. テストフック

`app.js` の [`installTestHooks`](../app.js#L14144) から末尾まで約1,915行あり、`window.__cadTest` に大量の fixture 作成・内部操作を公開する。`?test=1` のときだけインストールするが、コード自体は本番スクリプトに同梱される。

この方式は細かい内部検証に役立っている一方、次の問題がある。

- 本体ファイルの責務と行数を増やす。
- テストが内部関数・可変状態へ強く結合する。
- 実UI経路を通らない回帰テストが増えやすい。
- 本番とテスト支援コードの境界が見えにくい。

改善案は [改善バックログ](./91-改善バックログ.md) に記載する。

## 5. 検証コマンド

```powershell
npm run check
npm run test:unit
npm run test:e2e
npm run test:all
```

`npm run check` は JavaScript の構文検証であり、lint、型検査、Markdown link、JSON schema は検証しない。

`npm run test:e2e` は Playwright 全件を実行する。一部性能・ドラッグテストは複数ケースを1つの `test()` 内で回すため、表示件数より実行ケースが多い。

`npm run test:unit` はNode標準test runnerでpureなGeometry/Solver契約を検証する。`npm run test:all`は構文、unit、全E2Eを順に実行する。

## 6. 仕様更新時の確認項目

1. 実装済みと設計案を同じ断定表現で混ぜていないか。
2. 保存形式の変更は `version`、migration、round-trip テストを伴うか。
3. 新しい Geometry 参照は通常 Geometry、Block Projection、入れ子 Projection で同じように解決できるか。
4. Geometry 削除、Sketch 削除、Block 編集、構成無効化の全経路で参照ライフサイクルが一致するか。
5. 通常 Document と Block Editor の両スコープで動くか。
6. Undo/Redo、保存・読込、キャンセルの各境界をテストしたか。
7. Presentation の変更が solve 変数や Constraint を変更していないか。
