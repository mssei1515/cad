# AGENTS.md

このファイルは、このリポジトリで Codex が作業する際の共通ルールを定義する。

原則として、すべての変更タスクで本ルールに従うこと。

---

## 1. 基本方針

ユーザーから依頼された変更の目的を理解し、既存の設計・仕様・コードとの整合性を保ちながら実装すること。

必要以上に変更範囲を広げず、依頼された内容に必要な範囲で変更すること。

既存の仕様や実装を確認せずに、推測だけで新しい仕様を決めないこと。

---

## 2. 作業開始時の確認

変更を開始する前に、必要に応じて以下を確認すること。

- 関連する既存コード
- `spec/` 内の関連仕様書
- 関連するテスト
- 周辺機能との依存関係
- 現在の Git の変更状態

既存の未コミット変更が存在する場合、それが今回のタスクによる変更か、ユーザーが以前から行っていた変更かを区別すること。

今回のタスクと無関係な既存変更を、勝手に修正・削除・コミットしないこと。

---

## 3. 仕様判断

実装中に仕様上の判断が必要になった場合は、独断で仕様を決定せず、ユーザーに判断を仰ぐこと。

仕様判断が必要な例には、以下を含む。

- 複数の合理的な挙動が考えられる場合
- UIや操作方法について新しい振る舞いを決める必要がある場合
- データ構造や内部モデルについて新しい仕様を決める必要がある場合
- 新しい制約条件やルールを定義する必要がある場合
- 既存仕様から挙動を一意に読み取れない場合
- 既存仕様同士が矛盾している場合
- 修正によって既存のユーザー操作や仕様が変わる可能性がある場合

このような場合は、実装を進める前に以下を簡潔に提示すること。

1. 何について判断が必要なのか
2. 考えられる選択肢
3. 各選択肢の主な違い
4. 推奨案がある場合は、その案と理由

そのうえで、ユーザーの判断を仰ぐこと。

### 明らかなバグの場合

ただし、既存の仕様、コード、テスト、または明確な意図に照らして、明らかにバグである場合はユーザーへの確認を待つ必要はない。

その場合は、ただちに修正してよい。

明らかなバグの例には、以下を含む。

- 明文化された既存仕様と実装が一致していない
- 正常な操作によって例外やクラッシュが発生する
- 明らかな計算ミスがある
- 明らかな条件分岐ミスがある
- 明らかな参照先の間違いがある
- UIが意図せず崩れている
- 既存機能が変更意図なく動作しなくなっている
- 既存テストが示す期待動作を実装が満たしていない

バグを修正する過程で、新しい仕様を決める必要が生じた場合は、その時点でユーザーに判断を仰ぐこと。

**バグなのか仕様なのか明確に判断できない場合は、バグと決めつけず、ユーザーに確認すること。**

---

## 4. 実装

ユーザーから依頼された内容を実装すること。

実装時には以下を意識すること。

- 既存の設計思想を可能な限り維持する
- 不要なリファクタリングを同時に行わない
- 変更範囲を必要最小限に保つ
- 関連する既存機能を壊さない
- 同じ役割の処理を不必要に重複させない
- 将来の保守が困難になる一時的な回避策を安易に導入しない

依頼された問題とは別の問題を発見した場合、それが今回の変更に不可欠でなければ勝手に変更範囲を拡大しないこと。

---

## 5. `spec/` の位置付け

`spec/` は、このプロジェクトの**現在の正式な仕様を記録する場所**とする。

`spec/` は変更履歴を記録するための場所ではなく、「現在どのような仕様になっているか」を確認できる状態を維持すること。

コードと `spec/` の内容が矛盾した状態を残してはならない。

---

## 6. 仕様書の更新

実装によって以下のいずれかが変更された場合は、必ず `spec/` 内の関連する Markdown ファイルを更新すること。

- 機能
- 挙動
- UI
- 操作方法
- データ構造
- 制約条件
- 計算方法
- 状態遷移
- ユーザーから見える振る舞い
- 内部設計のうち仕様として保持すべき事項

対応する仕様書が存在しない場合は、必要に応じて新しい Markdown ファイルを作成すること。

仕様変更を伴わない純粋な内部修正や明らかなバグ修正についても、現在の `spec/` と実装に不一致がないことを確認すること。

実装だけを変更し、必要な仕様書更新を行わずにタスクを完了してはならない。

---

## 7. 検証

変更後は、変更内容に応じて可能な範囲で検証を行うこと。

利用可能な場合は、以下を実施すること。

- 自動テスト
- 関連する単体テスト
- lint
- type check
- build
- その他、このプロジェクトで定義されている検証

変更に対応する既存テストがある場合は、必ず実行すること。

必要性が高い場合は、テストの追加または更新も行うこと。

検証に失敗した場合は、原因を確認し、原則として問題を解決してからコミットすること。

既存の問題など、今回の変更とは無関係な理由によって検証が失敗する場合は、その事実を完了報告で明示すること。

---

## 8. コミット前の確認

コミットする前に `git diff` および Git の状態を確認すること。

少なくとも以下を確認すること。

- ユーザーが依頼した変更が含まれている
- 必要な `spec/` の更新が含まれている
- 必要なテスト変更が含まれている
- デバッグ用コードや不要なログが残っていない
- 一時ファイルが含まれていない
- 今回のタスクと無関係な変更が含まれていない

今回のタスク以前から存在するユーザーの未コミット変更を、勝手にコミットに含めないこと。

---

## Git ブランチ運用ルール

- `main` ブランチは正式リリース版として扱う。
- 通常の開発作業を `main` 上で直接行わない。
- 日常の開発は `develop` ブランチを基準として進める。
- 新機能や比較的大きな修正を行う場合は、原則として `develop` から作業用ブランチを作成する。

例：

- `feature/sketch-projection`
- `feature/hatching`
- `fix/save-error`

基本的な流れ：

`develop` → 作業用ブランチを作成 → 実装・テスト → `develop` にマージ

正式リリースする場合のみ：

`develop` → 動作確認・テスト → `main` にマージ

### `main` と `develop` の同期

- 通常開発中は、`main`が`develop`の祖先である状態を維持する。正式リリース直後は、両ブランチが同じcommitを指す状態まで同期する。
- 変更タスクの開始前および正式リリース前に、remoteの`main`と`develop`を取得し、`git merge-base --is-ancestor main develop`が成功することを確認する。成功しない場合は新しい変更や`develop`から`main`へのマージを開始せず、先に`main`の変更を`develop`へ統合し、必要な検証とPushを完了する。
- `develop`を`main`へマージして`main`をPushした後は、直ちに`develop`へ戻り、`git merge --ff-only main`でリリースmerge commitを取り込んで`develop`もPushする。最後にlocal／remoteの`main`と`develop`のcommit IDがそれぞれ一致すること、および`main`が`develop`の祖先であることを確認する。
- `--ff-only`による同期に失敗した場合は、force push、rebaseまたはresetで共有履歴を書き換えない。remote更新や分岐原因を調査し、`main`から`develop`への通常merge、競合解消、全体検証、Commit、Pushを行ってから次の開発へ進む。
- 機能変更や修正を`main`だけへ残してはならない。ユーザーの明示的な指示によって緊急修正を`main`へ直接反映した場合も、次の変更タスクを始める前に同じ変更を`develop`へ統合し、検証してPushする。

作業用ブランチの終了：

- `feature/*`、`fix/*` などの作業用ブランチは、マージと必要な検証が完了し、役目を終えた時点でローカルブランチとリモートブランチの両方を削除する。
- 削除前に、対象コミットが `develop` または正式なマージ先へ取り込まれていることを確認する。
- 未マージ、追加修正予定、調査中の作業用ブランチは削除しない。
- `main` と `develop` は長期維持するブランチとし、作業用ブランチとして削除しない。

エージェントは、ユーザーから明示的な指示がない限り、`main` に直接コミット・マージしないこと。

作業開始時には現在のブランチを確認し、`main` 上にいる場合は、そのまま開発を開始せず適切な開発ブランチへ切り替えること。

---

## 実行コミット情報ファイル

`file:///C:/dev/cad/index.html`から起動したJot2Dでも実行中のGit情報を表示できるように、repository直下の`runtime-version.js`を使用する。

- `runtime-version.js`は`npm run write:runtime-version`で現在のbranch、commit ID、working treeの変更有無から生成する。
- このファイルは生成物としてGit管理外に置き、手作業で編集したりcommitへ含めたりしない。
- 作業開始時のcheckout後、およびcommit・merge・branch切替によって`HEAD`が変わった後に再生成する。
- 変更タスクでは、最終commitの完了後かつ完了報告前に必ず再生成し、Help menuが最終commitを示す状態にする。
- commit後にtracked fileを変更した場合は、working treeの「変更あり」表示を一致させるため再生成する。
- `file:` URLで旧版のJavaScriptやcommit情報がブラウザキャッシュから再利用されないよう、`index.html`のscript読込には起動ごとに異なるqueryを付与する状態を維持する。
- HTTP serverは`runtime-version.js`と同じ形式の情報を動的に返すため、生成物をGit管理へ追加しない。

---

## 9. Commit

変更タスクが正常に完了したら、必ず Git にコミットすること。

原則として、ユーザーからの1つの変更依頼を1つの論理的なコミットとしてまとめること。

コミットには以下を含めること。

- 実装変更
- 対応する `spec/` の変更
- 必要なテスト変更

コミットメッセージは、変更内容が分かる簡潔なものにすること。

ユーザーが明示的にコミットしないよう指示した場合のみ、コミットを省略してよい。

---

## 10. Push

コミット後は、必ず現在のブランチをリモートリポジトリへ Push すること。

ユーザーは、`https://github.com/mssei1515/cad`を指す`origin`に対し、通常の変更タスク完了後に作業ブランチおよび`develop`をPushすることを継続的に許可している。この範囲のPushはタスクごとの追加確認なしで実行してよい。リモートURLが異なる場合や、`main`へのマージまたはPushはこの継続許可に含めず、従来どおりユーザーの明示的な指示を必要とする。

原則として、

**実装 → 仕様更新 → 検証 → Commit → Push**

までを1つの変更タスクとして扱うこと。

Push が以下のような理由で実行できない場合は、その理由をユーザーへ明確に報告すること。

- GitHub の認証が完了していない
- 権限がない
- ネットワークに接続できない
- リモート側との競合がある
- Git または GitHub CLI など必要な環境が不足している

Push に成功していない場合、成功したと報告してはならない。

ユーザーが明示的に Push しないよう指示した場合のみ、Push を省略してよい。

---

## 11. 問題が発生した場合

作業中に問題が発生した場合は、まず原因を調査すること。

修正方法が明確で、それが既存仕様の範囲内のバグ修正であれば、そのまま修正してよい。

一方で、解決方法によって仕様が変わる場合や複数の設計案が成立する場合は、ユーザーに判断を仰ぐこと。

問題を隠すためだけの暫定処置によって、タスクを完了したことにしてはならない。

---

## 12. 完了報告

作業完了時には、簡潔に以下を報告すること。

- 実装した内容
- 主な変更ファイル
- 更新した `spec/` ファイル
- 実施したテスト・検証
- テスト・検証結果
- Commit の内容
- Push の成否

未解決の問題、既知の制限、実行できなかった検証が存在する場合は、それも報告すること。

---

# Definition of Done

コードまたはプロジェクトファイルを変更するタスクは、原則として以下がすべて完了した時点で完了とみなす。

1. ユーザーから依頼された変更が実装されている
2. 必要な `spec/` が更新されている
3. 実装と `spec/` の内容が一致している
4. 必要なテスト・検証が完了している
5. コミット前の差分確認が完了している
6. Git に Commit されている
7. リモートリポジトリへ Push されている

上記を完了していない状態で、原則として「完了」として扱わないこと。

ただし、認証・権限・環境・外部サービスなど Codex 自身では解決できない理由によって Commit または Push 等を実行できない場合は、その理由と未完了の作業をユーザーへ明確に報告すること。

ユーザーから本ファイルのルールと異なる明示的な指示があった場合は、そのタスクについてはユーザーの最新の明示的な指示を優先すること。
