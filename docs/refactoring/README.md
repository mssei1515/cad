# 仕様整理と段階的リファクタリング

ここは仕様整理・移行・実装分離の開発資料。現在の正式仕様は[spec/README.md](../../spec/README.md)を参照する。未採用の草案を実装の根拠として使用しない。

## 目次案の位置づけ

「基本概念／共通契約／機能別操作／UI／データと互換性／計算契約／検証対応」の7領域を、仕様整理の到達先として採用する。各領域を複数の文書に分けた**移行先のファイル構成、現状との対応、作業順序**を[仕様の移行計画](./migration-plan.md)にまとめた。

これは文書構成の計画であり、製品の挙動や内部モデルを新たに決定するものではない。正式仕様の再配置はM1～M6で実施した。

## 現在地と次の作業

M1～M6の文書再配置を完了した。基本概念・共通契約・機能別操作・UI・データと互換性・計算契約・検証対応の7領域から、現在の正本を読める。旧番号付き文書はリンク互換用の案内として保持する。

次は実装分離に向けた準備。まず拘束選択エラーを共通テストfixtureが除外している箇所を調査・再現し、成功／失敗時の状態を保証するテストを整える。ユーザーが経験したエラー・遅延との同一性は未確認。並行して既存fixtureの性能基準値と呼出し経路を整理し、対象範囲を決めて段階的に分離する。

本番コードの分割、テスト削減、不具合解消、性能改善は未実施。文書の再配置完了と、製品の問題解決を分けて管理する。

## 資料の読み方

| 資料 | 役割 |
| --- | --- |
| [仕様の移行計画](./migration-plan.md) | 到達先のファイル構成、7領域の状態、残作業と終了条件 |
| [見出し別の移行台帳](./spec-inventory.md) | 現行の全見出しと移行先の対応。移動の取りこぼしを確認する |
| [共通契約の草案](./contracts.md) | 正式化前の比較・調査資料。採用済み部分は正式仕様を参照する |
| [整理候補と判断事項](./decisions.md) | ユーザー判断、仕様の不整合、未解決事項、テスト整理の候補 |
| [実装対応表](./implementation-map.md) | 現在のファイル責務と領域別テスト |
| [改善バックログ](./backlog.md) | 将来の技術改善候補。正式仕様ではない |
| [検証一覧の移行照合](./verification-audit.md) | 旧保証一覧60項目の移行先と照合用の過去記述。正式仕様ではない |
| [作業記録](./progress-log.md) | 過去に照合・検証した範囲と結果 |

## 記述の形

機能ごとに必要な項目だけを記載する。空欄を埋めるために仕様を増やさない。

1. 対象と前提条件
2. 入力・操作
3. 成功時の結果
4. 取消・失敗時の結果と復元範囲
5. 履歴・永続化
6. 共通契約への参照と固有の例外
7. 対応する検証

共通ルールの本文は一か所へ置く。概要からはリンクし、数値や条件を再掲しない。ルールには変更後も追跡できるIDを付け、テストはIDと保証内容を対応させる。草案のIDは正式仕様へ採用した時点で固定する。

## 実装分離との関係

対象領域の仕様・回帰テスト・現状の性能を揃えてから、操作の確定／復元、計算実行、依存更新などの責任を段階的に分離する。全仕様の完成を待って一括で書き直さない。

E2Eの削減と実装の大幅変更は同時に行わず、移した保証が新しいテストで成立してから旧テストを除く。文書の再配置だけで拘束エラーやドラッグ遅延が解消したとは扱わない。
